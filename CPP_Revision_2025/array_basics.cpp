#include<bits/stdc++.h>

using namespace std;

int main(){
    return 0;
}

/*
Arrays store same data type values. ex. if int arr[5] and say arr[1] = 7.7, then cout << arr[1] gives 7, not 7.7
Arrays stores all its values in consecutive memory locations.
Can change values in an array since arrays are mutable

if an array location is not defined, it returns a garbage value. ex. if int arr[5] and cout<<arr[3] without declaring,
random garbage value is returned */